[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "artux-muninn"
version = "3.1.0"
description = "Hybrid STM/LTM Memory System for AI Agents"
license = "MPL-2.0"

[tool.setuptools]
packages = ["memory_module"]

[tool.setuptools.package-dir]
memory_module = "."
